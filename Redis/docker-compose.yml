version: '2'
services:
    redis:
        image: redis
        container_name: cslab_redis
        volumes:
          - ./.data/redis:/data
          - ./conf/redis.conf:/usr/local/etc/redis/redis.conf
        ports:
          - 6379:6379
